///////////////////////////////////////////////////////////
//  AssayclassResolver.java
//  Macromedia ActionScript Implementation of the Class AssayclassResolver
//  Generated by Enterprise Architect
//  Created on:      17-May-2020 3:09:16 PM
//  Original author: EBRIONES
///////////////////////////////////////////////////////////

package org.ebs.graphql.resolvers;

import com.coxautodev.graphql.tools.GraphQLResolver;
import org.springframework.stereotype.Component;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.validation.annotation.Validated;
import java.util.stream.Collectors;
import java.util.List;
import java.util.Optional;
import java.util.Set;
import org.springframework.core.convert.ConversionService;
import org.ebs.model.AssayclassModel;
import org.ebs.services.to.AssayclassTo;
import org.ebs.model.repos.AssayclassRepository;
import org.ebs.services.AssayclassService;
import org.ebs.services.TechnologyPlatformService;
import org.ebs.model.repos.TechnologyPlatformRepository;
import org.ebs.services.to.TechnologyPlatformTo;
import org.ebs.services.ServiceService;
import org.ebs.model.repos.ServiceRepository;
import org.ebs.services.to.ServiceTo;

/**
 * @author EBRIONES
 * @version 1.0
 * @created 17-May-2020 3:09:16 PM
 */
@Component @Validated
public class AssayclassResolver implements GraphQLResolver<AssayclassTo> {

	private AssayclassService assayclassService;
	private AssayclassRepository assayclassRepository;
	@Autowired
	private ConversionService converter;
	private TechnologyPlatformService technologyplatformService;
	private TechnologyPlatformRepository technologyplatformRepository;
	private ServiceService serviceService;
	private ServiceRepository serviceRepository;

	/**
	 * 
	 * @param serviceRepository
	 * @param serviceService
	 * @param technologyplatformRepository
	 * @param technologyplatformService
	 * @param assayclassRepository
	 * @param assayclassService
	 */
	@Autowired
	public AssayclassResolver(ServiceRepository serviceRepository, ServiceService serviceService, TechnologyPlatformRepository technologyplatformRepository, TechnologyPlatformService technologyplatformService, AssayclassRepository assayclassRepository, AssayclassService assayclassService){
		this.assayclassService = assayclassService; 
		this.assayclassRepository = assayclassRepository; 
		this.technologyplatformService = technologyplatformService; 
		this.technologyplatformRepository = technologyplatformRepository; 
		this.serviceService = serviceService; 
		this.serviceRepository = serviceRepository; 
	
	}

	/**
	 * 
	 * @param assayclassTo
	 */
	public ServiceTo getService(AssayclassTo assayclassTo){
		AssayclassModel assayclassModel = assayclassRepository.findById(assayclassTo.getId()).get(); 
		 return serviceService.findService(assayclassModel.getService().getId()).get();
	}

}