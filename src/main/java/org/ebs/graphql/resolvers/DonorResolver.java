///////////////////////////////////////////////////////////
//  DonorResolver.java
//  Macromedia ActionScript Implementation of the Class DonorResolver
//  Generated by Enterprise Architect
//  Created on:      28-Apr-2020 11:58:13 PM
//  Original author: EBRIONES
///////////////////////////////////////////////////////////

package org.ebs.graphql.resolvers;

import com.coxautodev.graphql.tools.GraphQLResolver;
import org.springframework.stereotype.Component;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.validation.annotation.Validated;
import java.util.stream.Collectors;
import java.util.List;
import java.util.Optional;
import java.util.Set;
import org.springframework.core.convert.ConversionService;
import org.ebs.model.DonorModel;
import org.ebs.services.to.DonorTo;
import org.ebs.model.repos.DonorRepository;
import org.ebs.services.DonorService;
import org.ebs.services.PearsonService;
import org.ebs.model.repos.PearsonRepository;
import org.ebs.services.to.PearsonTo;

/**
 * @author EBRIONES
 * @version 1.0
 * @created 28-Apr-2020 11:58:13 PM
 */
@Component @Validated
public class DonorResolver implements GraphQLResolver<DonorTo> {

	private DonorService donorService;
	private DonorRepository donorRepository;
	@Autowired
	private ConversionService converter;
	private PearsonService pearsonService;
	private PearsonRepository pearsonRepository;

	/**
	 * 
	 * @param pearsonRepository
	 * @param pearsonService
	 * @param donorRepository
	 * @param donorService
	 */
	@Autowired
	public DonorResolver(PearsonRepository pearsonRepository, PearsonService pearsonService, DonorRepository donorRepository, DonorService donorService){
		this.donorService = donorService; 
		this.donorRepository = donorRepository; 
		this.pearsonService = pearsonService; 
		this.pearsonRepository = pearsonRepository; 
	
	}

	/**
	 * 
	 * @param donorTo
	 */
	public PearsonTo getPearson(DonorTo donorTo){
		DonorModel donorModel = donorRepository.findById(donorTo.getId()).get(); 
		 return pearsonService.findPearson(donorModel.getPearson().getId()).get();
	}

}