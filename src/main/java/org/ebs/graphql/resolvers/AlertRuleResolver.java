///////////////////////////////////////////////////////////
//  AlertRuleResolver.java
//  Macromedia ActionScript Implementation of the Class AlertRuleResolver
//  Generated by Enterprise Architect
//  Created on:      17-May-2020 3:09:14 PM
//  Original author: EBRIONES
///////////////////////////////////////////////////////////

package org.ebs.graphql.resolvers;

import org.ebs.model.AlertRuleModel;
import org.ebs.model.repos.AlertRepository;
import org.ebs.model.repos.AlertRuleRepository;
import org.ebs.services.AlertRuleService;
import org.ebs.services.AlertService;
import org.ebs.services.to.AlertRuleTo;
import org.ebs.services.to.AlertTo;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.core.convert.ConversionService;
import org.springframework.stereotype.Component;
import org.springframework.validation.annotation.Validated;

import com.coxautodev.graphql.tools.GraphQLResolver;

/**
 * @author EBRIONES
 * @version 1.0
 * @created 17-May-2020 3:09:14 PM
 */
@Component @Validated
public class AlertRuleResolver implements GraphQLResolver<AlertRuleTo> {

	private AlertRuleService alertruleService;
	private AlertRuleRepository alertruleRepository;
	@Autowired
	private ConversionService converter;
	private AlertService alertService;
	private AlertRepository alertRepository;

	/**
	 * 
	 * @param alertRepository
	 * @param alertService
	 * @param alertruleRepository
	 * @param alertruleService
	 */
	@Autowired
	public AlertRuleResolver(AlertRepository alertRepository, AlertService alertService, AlertRuleRepository alertruleRepository, AlertRuleService alertruleService){
		this.alertruleService = alertruleService; 
		this.alertruleRepository = alertruleRepository; 
		this.alertService = alertService; 
		this.alertRepository = alertRepository; 
	
	}

	/**
	 * 
	 * @param alertruleTo
	 */
	public AlertTo getAlert(AlertRuleTo alertruleTo){
		AlertRuleModel alertruleModel = alertruleRepository.findById(alertruleTo.getId()).get(); 
		 return alertService.findAlert(alertruleModel.getAlert().getId()).get();
	}

}