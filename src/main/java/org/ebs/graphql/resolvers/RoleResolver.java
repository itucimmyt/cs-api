///////////////////////////////////////////////////////////
//  RoleResolver.java
//  Macromedia ActionScript Implementation of the Class RoleResolver
//  Generated by Enterprise Architect
//  Created on:      27-Apr-2020 9:47:51 AM
//  Original author: EBRIONES
///////////////////////////////////////////////////////////

package org.ebs.graphql.resolvers;

import com.coxautodev.graphql.tools.GraphQLResolver;
import org.springframework.stereotype.Component;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.validation.annotation.Validated;
import java.util.stream.Collectors;
import java.util.List;
import java.util.Optional;
import java.util.Set;
import org.springframework.core.convert.ConversionService;
import org.ebs.model.RoleModel;
import org.ebs.services.to.RoleTo;
import org.ebs.model.repos.RoleRepository;
import org.ebs.services.RoleService;
import org.ebs.services.UserService;
import org.ebs.model.repos.UserRepository;
import org.ebs.services.to.UserTo;
import org.ebs.model.repos.ActionRepository;
import org.ebs.services.ActionService;
import org.ebs.services.to.ActionTo;

/**
 * @author EBRIONES
 * @version 1.0
 * @created 27-Apr-2020 9:47:51 AM
 */
@Component @Validated
public class RoleResolver implements GraphQLResolver<RoleTo> {

	private ActionRepository actionRepository;
	private ActionService actionService;
	@Autowired
	private ConversionService converter;
	private RoleRepository roleRepository;
	private RoleService roleService;
	private UserRepository userRepository;
	private UserService userService;

	/**
	 * 
	 * @param roleTo
	 */
	public Set<ActionTo> getActions(RoleTo roleTo){
		return actionRepository.findByRoleId(roleTo.getId()).stream() 
		 .map(e -> converter.convert(e,ActionTo.class)) 
		 .collect(Collectors.toSet());
	}

	/**
	 * 
	 * @param roleTo
	 */
	/*public UserTo getUser(RoleTo roleTo){
		RoleModel roleModel = roleRepository.findById(roleTo.getId()).get(); 
		 return userService.findUser(roleModel.getUser().getId()).get();
	}*/

	/**
	 * 
	 * @param actionRepository
	 * @param actionService
	 * @param userRepository
	 * @param userService
	 * @param roleRepository
	 * @param roleService
	 */
	@Autowired
	public RoleResolver(ActionRepository actionRepository, ActionService actionService, UserRepository userRepository, UserService userService, RoleRepository roleRepository, RoleService roleService){
		this.roleService = roleService; 
		this.roleRepository = roleRepository; 
		this.userService = userService; 
		this.userRepository = userRepository; 
		this.actionService = actionService; 
		this.actionRepository = actionRepository; 
	
	}

}