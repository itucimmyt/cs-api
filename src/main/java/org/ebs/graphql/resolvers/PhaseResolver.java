///////////////////////////////////////////////////////////
//  PhaseResolver.java
//  Macromedia ActionScript Implementation of the Class PhaseResolver
//  Generated by Enterprise Architect
//  Created on:      17-May-2020 3:09:55 PM
//  Original author: EBRIONES
///////////////////////////////////////////////////////////

package org.ebs.graphql.resolvers;

import com.coxautodev.graphql.tools.GraphQLResolver;
import org.springframework.stereotype.Component;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.validation.annotation.Validated;
import java.util.stream.Collectors;
import java.util.List;
import java.util.Optional;
import java.util.Set;
import org.springframework.core.convert.ConversionService;
import org.ebs.model.PhaseModel;
import org.ebs.services.to.PhaseTo;
import org.ebs.model.repos.PhaseRepository;
import org.ebs.services.PhaseService;
import org.ebs.model.repos.SchemeRepository;
import org.ebs.services.SchemeService;
import org.ebs.services.to.SchemeTo;
import org.ebs.services.PipelineService;
import org.ebs.model.repos.PipelineRepository;
import org.ebs.services.to.PipelineTo;

/**
 * @author EBRIONES
 * @version 1.0
 * @created 17-May-2020 3:09:55 PM
 */
@Component @Validated
public class PhaseResolver implements GraphQLResolver<PhaseTo> {

	private PhaseService phaseService;
	private PhaseRepository phaseRepository;
	@Autowired
	private ConversionService converter;
	private SchemeService schemeService;
	private SchemeRepository schemeRepository;
	private PipelineService pipelineService;
	private PipelineRepository pipelineRepository;

	/**
	 * 
	 * @param phaseTo
	 */
	public PipelineTo getPipeline(PhaseTo phaseTo){
		PhaseModel phaseModel = phaseRepository.findById(phaseTo.getId()).get(); 
		 return pipelineService.findPipeline(phaseModel.getPipeline().getId()).get();
	}

	/**
	 * 
	 * @param pipelineRepository
	 * @param pipelineService
	 * @param schemeRepository
	 * @param schemeService
	 * @param phaseRepository
	 * @param phaseService
	 */
	@Autowired
	public PhaseResolver(PipelineRepository pipelineRepository, PipelineService pipelineService, SchemeRepository schemeRepository, SchemeService schemeService, PhaseRepository phaseRepository, PhaseService phaseService){
		this.phaseService = phaseService; 
		this.phaseRepository = phaseRepository; 
		this.schemeService = schemeService; 
		this.schemeRepository = schemeRepository; 
		this.pipelineService = pipelineService; 
		this.pipelineRepository = pipelineRepository; 
	
	}

}