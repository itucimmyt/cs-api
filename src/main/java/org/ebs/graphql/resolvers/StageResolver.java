///////////////////////////////////////////////////////////
//  StageResolver.java
//  Macromedia ActionScript Implementation of the Class StageResolver
//  Generated by Enterprise Architect
//  Created on:      17-May-2020 3:10:21 PM
//  Original author: EBRIONES
///////////////////////////////////////////////////////////

package org.ebs.graphql.resolvers;

import org.ebs.model.repos.SchemaRepository;
import org.ebs.model.repos.SchemeRepository;
import org.ebs.model.repos.StageRepository;
import org.ebs.services.SchemaService;
import org.ebs.services.SchemeService;
import org.ebs.services.StageService;
import org.ebs.services.to.StageTo;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.core.convert.ConversionService;
import org.springframework.stereotype.Component;
import org.springframework.validation.annotation.Validated;

import com.coxautodev.graphql.tools.GraphQLResolver;

/**
 * @author EBRIONES
 * @version 1.0
 * @created 17-May-2020 3:10:21 PM
 */
@Component @Validated
public class StageResolver implements GraphQLResolver<StageTo> {

	private StageService stageService;
	private StageRepository stageRepository;
	@Autowired
	private ConversionService converter;
	private SchemeService schemeService;
	private SchemeRepository schemeRepository;
	private SchemaService schemaService;
	private SchemaRepository schemaRepository;

	/**
	 * 
	 * @param schemaRepository
	 * @param schemaService
	 * @param schemeRepository
	 * @param schemeService
	 * @param stageRepository
	 * @param stageService
	 */
	@Autowired
	public StageResolver(SchemaRepository schemaRepository, SchemaService schemaService, SchemeRepository schemeRepository, SchemeService schemeService, StageRepository stageRepository, StageService stageService){
		this.stageService = stageService; 
		this.stageRepository = stageRepository; 
		this.schemeService = schemeService; 
		this.schemeRepository = schemeRepository; 
		this.schemaService = schemaService; 
		this.schemaRepository = schemaRepository; 
	
	}

}