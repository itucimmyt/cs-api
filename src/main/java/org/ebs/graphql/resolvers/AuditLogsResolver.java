///////////////////////////////////////////////////////////
//  AuditLogsResolver.java
//  Macromedia ActionScript Implementation of the Class AuditLogsResolver
//  Generated by Enterprise Architect
//  Created on:      17-May-2020 3:09:20 PM
//  Original author: EBRIONES
///////////////////////////////////////////////////////////

package org.ebs.graphql.resolvers;

import com.coxautodev.graphql.tools.GraphQLResolver;
import org.springframework.stereotype.Component;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.validation.annotation.Validated;
import java.util.stream.Collectors;
import java.util.List;
import java.util.Optional;
import java.util.Set;
import org.springframework.core.convert.ConversionService;
import org.ebs.model.AuditLogsModel;
import org.ebs.services.to.AuditLogsTo;
import org.ebs.model.repos.AuditLogsRepository;
import org.ebs.services.AuditLogsService;
import org.ebs.services.InstanceService;
import org.ebs.model.repos.InstanceRepository;
import org.ebs.services.to.InstanceTo;

/**
 * @author EBRIONES
 * @version 1.0
 * @created 17-May-2020 3:09:20 PM
 */
@Component @Validated
public class AuditLogsResolver implements GraphQLResolver<AuditLogsTo> {

	private AuditLogsService auditlogsService;
	private AuditLogsRepository auditlogsRepository;
	@Autowired
	private ConversionService converter;
	private InstanceService instanceService;
	private InstanceRepository instanceRepository;

	/**
	 * 
	 * @param instanceRepository
	 * @param instanceService
	 * @param auditlogsRepository
	 * @param auditlogsService
	 */
	@Autowired
	public AuditLogsResolver(InstanceRepository instanceRepository, InstanceService instanceService, AuditLogsRepository auditlogsRepository, AuditLogsService auditlogsService){
		this.auditlogsService = auditlogsService; 
		this.auditlogsRepository = auditlogsRepository; 
		this.instanceService = instanceService; 
		this.instanceRepository = instanceRepository; 
	
	}

	/**
	 * 
	 * @param auditlogsTo
	 */
	public InstanceTo getInstance(AuditLogsTo auditlogsTo){
		AuditLogsModel auditlogsModel = auditlogsRepository.findById(auditlogsTo.getId()).get(); 
		 return instanceService.findInstance(auditlogsModel.getInstance().getId()).get();
	}

}